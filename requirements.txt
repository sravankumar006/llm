# PyTorch CPU wheel index (prevents downloading multi-gigabyte CUDA binaries locally)
--extra-index-url https://download.pytorch.org/whl/cpu

# Deep Learning Framework (CPU optimized for local development)
torch>=2.1.0

# Numerical Computing & Tokenization
numpy>=1.24.0,<2.0.0
tiktoken>=0.5.0
transformers>=4.35.0

# Data Engineering & Utilities
datasets>=2.14.0
tqdm>=4.66.0

# Inference Backend (FastAPI + High-Performance ASGI)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0
